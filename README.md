# NormalServletOperations
* we have create a servlet and a components and all the values are updates using servlet path.
* We have used TextComponent using js use api
* We have used newComponent and accessing all its data using properties.
*******************
* dataStore component:just call UserModel in this which is just pojo class
* DataStoreServlet contains the path of the page in which we are storing data 
* and also the path of the servlet and whenever we are authoring it through our cq:dialogue
* the changes are reverted at localhost:4502/bin/submit2
